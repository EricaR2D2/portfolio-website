document.addEventListener('DOMContentLoaded', function() {
    // Projects functionality is disabled until new projects are ready to be added
    // The placeholder message is directly in the HTML
});
